diff -rNu a/drivers/usb/dwc3/core.c b/drivers/usb/dwc3/core.c
--- a/drivers/usb/dwc3/core.c	2022-03-08 20:09:39.000000000 +0200
+++ b/drivers/usb/dwc3/core.c	2022-10-03 13:28:15.742759044 +0300
@@ -1519,9 +1519,13 @@
 		return PTR_ERR(dwc->reset);
 
 	if (dev->of_node) {
-		ret = devm_clk_bulk_get_all(dev, &dwc->clks);
-		if (ret == -EPROBE_DEFER)
-			return ret;
+		if (dev->of_node->parent && of_property_read_bool(dev->of_node->parent, "no-clk_bulk_get")) {
+			ret = -ENOENT;
+		} else {
+			ret = devm_clk_bulk_get_all(dev, &dwc->clks);
+			if (ret == -EPROBE_DEFER)
+				return ret;
+		}
 		/*
 		 * Clocks are optional, but new DT platforms should support all
 		 * clocks as required by the DT-binding.
diff -rNu a/drivers/usb/dwc3/dwc3-qcom.c b/drivers/usb/dwc3/dwc3-qcom.c
--- a/drivers/usb/dwc3/dwc3-qcom.c	2022-03-08 20:09:39.000000000 +0200
+++ b/drivers/usb/dwc3/dwc3-qcom.c	2022-10-03 13:21:18.702210385 +0300
@@ -780,11 +780,14 @@
 		ret = PTR_ERR(qcom->qscratch_base);
 		goto clk_disable;
 	}
-
-	ret = dwc3_qcom_setup_irq(pdev);
-	if (ret) {
-		dev_err(dev, "failed to setup IRQs, err=%d\n", ret);
-		goto clk_disable;
+	if (np && of_property_read_bool(np, "no-dwc3_qcom_setup_irq")) {
+		ret = 0;
+	} else {
+		ret = dwc3_qcom_setup_irq(pdev);
+		if (ret) {
+			dev_err(dev, "failed to setup IRQs, err=%d\n", ret);
+			goto clk_disable;
+		}
 	}
 
 	/*
